#!/usr/bin/env php -q
<?php

require_once __DIR__ . '/../vendor/autoload.php';

use robyj\csv2meta\Commands\Conversion;
use Symfony\Component\Console\Application;

$app = new Application();
$app->add(new Conversion());

$app -> run();
